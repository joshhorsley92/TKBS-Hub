// Cockpit — placeholder until Phase 1c assembles the real thing in the
// Phase-D design. Proves the authed shell + data plumbing works.
export default function CockpitPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Cockpit</h1>
      <p className="mt-2 text-sm text-ink-muted">
        Phase 0 shell. Coming in Phase 1: now-strip, KPI row, feed, repo
        activity, client status.
      </p>
    </div>
  );
}
